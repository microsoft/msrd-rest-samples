{
    "msrdOrigin": "Replace with the API origin of MSRD deployment environment",
    "msrdAccountId": "Replace with MSRD account ID",
    "msrdApiToken": "Replace with MSRD API token",
    "azureStorageAccountId": "Replace with Azure Storage account ID", // Optional when File PUT API is used.
    "azureStorageApiKey": "Replace with Azure Storage API key",// Optional when File PUT API is used.
    "azureStorageContainer": "Replace with the name of an Azure Storage container, which will be created if needed",// Optional when File PUT API is used.
    // Optional. Add (with this schema) if needed.
    "proxies": {
        "http": "127.0.0.1:8888",
        "https": "127.0.0.1:8888"
    },
    // Optional. Can be `true`, `false`, or a path to a PEM-encoded root cert or bundle.
    "verifyCerts": "/path/to/my/cert.pem"
}
